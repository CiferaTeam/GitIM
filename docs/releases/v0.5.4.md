# GitIM v0.5.4

## 修复
- runtime daemon 关停更稳健，避免残留进程
- Claude context window 强制 200k，防止超限
- token/email 传播改到 daemon recovery 之前执行
- 为历史 workspace 自动补齐 noreply email
- sync_loop 在 `/user.email` 为 null 时仍能正确处理

## 改进
- 提示词采用 AIDA 风格路由线索，频道拆分规则强化"上下文稀缺"原则
