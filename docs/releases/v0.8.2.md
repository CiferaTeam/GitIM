# GitIM v0.8.2

## 新功能
- 草稿发送前可预览将被路由到的 recipients

## 修复
- Kimi provider 移除 idle completion fallback，避免误触发
- Cursor provider 修复 overflow reset fallback 逻辑
- daemon 的 read 响应正确包含 recipients 字段
- 浏览器端消息按 recipients 正确路由，不再广播
- 前端 provider usage 显示正确累计 totals
