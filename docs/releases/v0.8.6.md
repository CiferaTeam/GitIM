# GitIM v0.8.6

## 新功能

- Claude provider 支持 CLI 进程常驻，跨 turn 复用 prompt cache，减少冷启开销

## 修复

- 修复 gitim-core 和 gitim-agent-provider 的 clippy 编译阻断问题
- 修复 agent_loop 和 cursor 模块中的 collapsible_match 警告
- 移除越界的 admin 测试，修复相关编译错误

## 改进

- 全部 agent-provider 模块统一使用 preconditions helpers 替代裸 allow 属性，提升代码安全性
